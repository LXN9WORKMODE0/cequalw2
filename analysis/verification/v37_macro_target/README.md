# V37 verification snapshot

`macro_target_summary.csv` 是 JDAY 44430.0–44436.5 fresh run 的只读汇总。完整 5927 条 marker 和案例输出保存在被 Git 忽略的 `analysis/.runs/v37_macro_target/`。

该快照只证明现有 BHT 单断面闭合与逐断面积分闭合都不能原样激活；其中的 `n≈0.046` 是在内部模拟断面状态保持不变时反推的工程等效量，不是物理糙率认定。
